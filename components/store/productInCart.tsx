import styles from '../../styles/store.module.css'
import * as React from 'react'
import {CartProduct} from '../../models/product'

// TODO: Implement items in cart
export default function ProductInCart({ props }: { props: CartProduct }) {
    return (
        <div></div>
    );
}